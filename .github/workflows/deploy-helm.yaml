name: Deploy to Kubernetes With Helm

on:
  workflow_call:
    inputs:
      short_sha:
        type: string
        description: "The short image tag SHA (optional; defaults to latest)"
      environment:
        type: string
        description: "Deployment environment (e.g. production, preview)"
        required: true
      chart:
        type: string
        required: true
        description: "Helm chart to deploy (e.g., cabrera-evil/deploy-chart)"
      release_name:
        type: string
        required: true
        description: "Helm release name (e.g., api)"
      namespace:
        type: string
        required: true
        description: "Kubernetes namespace to deploy to"
      tailscale:
        type: boolean
        description: "Connect to Tailscale before deploying"
        default: false
      tailscale_tags:
        type: string
        description: "Comma-separated Tailscale tags applied to the ephemeral node (e.g. tag:ci)"
        default: ""
    outputs:
      status:
        description: "The status of the deployment"
        value: ${{ jobs.deploy.outputs.status }}
      url:
        description: "The URL of the ingress resource created by the Helm chart"
        value: ${{ jobs.deploy.outputs.url }}
    secrets:
      KUBECONFIG:
        required: true
        description: "Base64-encoded kubeconfig file"
      HELM_VALUES:
        required: true
        description: "Base64-encoded Helm values.yaml file"
      TS_OAUTH_CLIENT_ID:
        description: "Tailscale OAuth client ID"
        required: false
      TS_OAUTH_SECRET:
        description: "Tailscale OAuth client secret"
        required: false
      TAILSCALE_AUTHKEY:
        description: "Tailscale auth key (alternative to OAuth)"
        required: false

jobs:
  deploy:
    name: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      status: ${{ job.status }}
      url: ${{ steps.check.outputs.url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Tailscale
        if: ${{ inputs.tailscale }}
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          tags: ${{ inputs.tailscale_tags }}

      - name: Prepare Kubeconfig and Helm values
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          echo "${{ secrets.HELM_VALUES }}" | base64 -d > values.yaml
          chmod 600 ~/.kube/config

      - name: Set up Helm
        uses: azure/setup-helm@v5.0.0

      - name: Add Helm Repo
        run: |
          helm repo add cabrera-evil https://cabrera-evil.github.io/charts/
          helm repo update

      - name: Deploy with Helm
        id: deployment
        run: |
          IMAGE_TAG="${{ inputs.short_sha && format('sha-{0}', inputs.short_sha) || 'latest' }}"
          helm upgrade --install ${{ inputs.release_name }} \
            ${{ inputs.chart }} \
            --namespace "${{ inputs.namespace }}-${{ inputs.environment }}" \
            --create-namespace \
            --values values.yaml \
            --set image.tag=$IMAGE_TAG \

      - name: Check Deployment Status
        id: check
        run: |
          RELEASE_NAME="${{ inputs.release_name }}"
          NAMESPACE="${{ inputs.namespace }}-${{ inputs.environment }}"

          echo "Identifying Kubernetes deployment associated with Helm release '${RELEASE_NAME}' in namespace '${NAMESPACE}'..."
          DEPLOYMENT=$(kubectl get deploy -n "$NAMESPACE" \
            -l "app.kubernetes.io/instance=$RELEASE_NAME" \
            -o jsonpath="{.items[0].metadata.name}")

          if [ -z "$DEPLOYMENT" ]; then
            echo "Error: No matching deployment found for Helm release '${RELEASE_NAME}' in namespace '${NAMESPACE}'."
            exit 1
          fi

          echo "Deployment '${DEPLOYMENT}' identified. Monitoring rollout status..."
          kubectl rollout status deployment/"$DEPLOYMENT" -n "$NAMESPACE" --timeout=10m

          echo "Checking for associated Ingress resource..."
          INGRESS=$(kubectl get ingress -n "$NAMESPACE" \
            -l "app.kubernetes.io/instance=$RELEASE_NAME" \
            -o jsonpath="{.items[0].metadata.name}")

          if [ -n "$INGRESS" ]; then
            echo "Ingress resource '${INGRESS}' found. Extracting URL..."

            HOST=$(kubectl get ingress "$INGRESS" -n "$NAMESPACE" -o jsonpath="{.spec.rules[0].host}")
            PATH=$(kubectl get ingress "$INGRESS" -n "$NAMESPACE" -o jsonpath="{.spec.rules[0].http.paths[0].path}")
            [[ -z "$PATH" ]] && PATH="/"

            # Save to GitHub outputs
            echo "url=https://$HOST$PATH" >> $GITHUB_OUTPUT
          else
            echo "No Ingress resource found for Helm release '${RELEASE_NAME}'."
          fi
